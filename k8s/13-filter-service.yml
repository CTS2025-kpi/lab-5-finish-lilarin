apiVersion: v1
kind: Service
metadata:
  name: filter-service
  namespace: lab2-microservices
spec:
  type: ClusterIP
  selector:
    app: filter-service
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
