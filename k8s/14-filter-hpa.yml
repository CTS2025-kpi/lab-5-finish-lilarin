apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: filter-service-hpa
  namespace: lab2-microservices
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: filter-service
  minReplicas: 1
  maxReplicas: 3
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
